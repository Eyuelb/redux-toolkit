{
  "name": "redux-toolkit",
  "version": "1.0.0",
  "description": "this package will hold the configuration of reduk-toolkit letter to be used to inject to a project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ALLORA-GROUPS",
  "dependencies": {
    "@reduxjs/toolkit": "^1.9.7",
    "react-redux": "^8.1.3"
  },
  "devDependencies": {
    "@types/node": "^20.8.2"
  } 
}
